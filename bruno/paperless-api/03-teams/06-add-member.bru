meta {
  name: Add team member
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/teams/{{teamId}}/members
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "user_id": {{userId}},
    "role": "MEMBER"
  }
}
