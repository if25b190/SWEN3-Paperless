meta {
  name: READ_WRITE member cannot update team
  type: http
  seq: 7
}

put {
  url: {{baseUrl}}/teams/{{teamId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{memberAccessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Member must not update this team",
    "description": "Expected to be forbidden"
  }
}

tests {
  test("a READ_WRITE member cannot mutate the team", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
