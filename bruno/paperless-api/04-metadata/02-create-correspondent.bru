meta {
  name: Create correspondent
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/correspondents
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Bruno Integration Correspondent",
    "notes": "Created by the Bruno collection"
  }
}

script:post-response {
  bru.setVar("correspondentId", res.getBody().id);
}
