meta {
  name: Current user without token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: none
}

tests {
  test("current user requires authentication", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
